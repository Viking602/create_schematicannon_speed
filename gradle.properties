org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false
org.gradle.parallel=true

# Mod Info
mod_id=lantudapao
mod_name=Lantu Dapao
mod_version=1.0.0
mod_authors=Viking
mod_description=Modify Create Schematicannon print speed (300x faster by default)

# Minecraft & NeoForge
minecraft_version=1.21.1
neo_version=21.1.200

# Create (version format: major.minor.patch-buildNumber)
# 编译依赖版本（开发时使用最新版）
create_version=6.0.9-188
# 最低支持版本（运行时兼容 6.0.8+）
create_version_min=6.0.8
